package me.XXX.eesearcher;

/**
 * Program main class
 */
public class Main {

    /**
     * Start the application. We defer actual initialization to
     * {@link MainApplication#main(String[])} as a workaround to Java
     * not finding JavaFX in the classpath. JavaFX is bundled into this jar.
     *
     */
    public static void main(String[] args) {
        MainApplication.main(args);
    }

}
